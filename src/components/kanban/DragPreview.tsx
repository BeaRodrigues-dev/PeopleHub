import { Avatar, Box, Card, Chip, Stack, Typography } from '@mui/material';
import type { Candidate } from '../../types/candidate';
export function DragPreview({candidate}:{candidate:Candidate}) { return <Card sx={{width:326,p:2,borderRadius:3,boxShadow:'0 20px 42px rgba(44,38,106,.32)',transform:'rotate(1.5deg) scale(1.03)',cursor:'grabbing'}}><Stack direction="row" spacing={1.25}><Avatar src={candidate.avatar} sx={{width:48,height:48}}/><Box sx={{minWidth:0,flex:1}}><Typography noWrap fontWeight={800}>{candidate.name}</Typography><Typography noWrap variant="body2" color="text.secondary">{candidate.position}</Typography><Stack direction="row" gap={.6} sx={{mt:.8}}>{candidate.skills.slice(0,2).map(skill=><Chip key={skill} label={skill} size="small" sx={{height:21,fontSize:10}}/>)}</Stack></Box></Stack></Card>; }
